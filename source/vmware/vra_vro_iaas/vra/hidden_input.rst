Hidden input
*******************************

If you use the "Design" section of vRa to set some default value on some of your resourceAction or xaas blueprint input (like Default value -> Field -> Request Info -> Business Group -> Name), it will disappear from the get template vRa api call result....