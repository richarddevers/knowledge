Get source request (api or portal?)
************************************

This trick will set a form_gui boolean which will be set to true if the request come from the vRa portal and false if it have been directly issued through api.

.. image:: source.png